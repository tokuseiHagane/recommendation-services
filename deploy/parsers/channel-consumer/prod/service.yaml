apiVersion: v1
kind: Service
metadata:
  name: tg-channel-consumer-prod-service
  namespace: parsers
  labels:
    app: tg-channel-consumer-prod
spec:
  selector:
    app: tg-channel-consumer-prod
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
