apiVersion: v1
kind: Secret
metadata:
  name: tg-channel-consumer-prod-secret
  namespace: parsers
  labels:
    app: tg-channel-consumer-prod
    version: prod
type: Opaque
stringData:
  # NEVER commit real prod credentials. Fill on the cluster via kubectl edit / create.
  TG_DB_PASSWORD: "CHANGE_ME"
  VK_DB_PASSWORD: "CHANGE_ME"

  LOGFIRE_TOKEN: ""
