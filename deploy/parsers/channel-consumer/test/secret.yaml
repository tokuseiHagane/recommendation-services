apiVersion: v1
kind: Secret
metadata:
  name: tg-channel-consumer-test-secret
  namespace: parsers
  labels:
    app: tg-channel-consumer-test
    version: test
type: Opaque
stringData:
  # VK-only контур: TG_DB_PASSWORD не нужен (ENABLE_TG_MODULE=false, defaults в Settings).
  # Тот же root-пароль, что и у vkparser-test-secret.DATABASE_URL.
  VK_DB_PASSWORD: "XjcZYWHuxnxU"

  # Logfire (оставить пустым если не используется)
  LOGFIRE_TOKEN: ""
